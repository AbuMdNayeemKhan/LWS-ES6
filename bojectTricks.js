let person = {
    name: "Nayeem Khan",
    age: 25,
    Ocopation: "Developer",
    Bron: 1998,
    live: "Dhaka"
}

//print object keys
console.log(Object.keys(person));

//print object values
let objVal = Object.values(person);
console.log(objVal);

//print object keys and values 
let keysValues = Object.entries(person);
console.log(keysValues);